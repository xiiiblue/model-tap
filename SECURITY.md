# 安全策略

## 报告安全问题

请不要在公开Issue中提交可利用漏洞、有效API Key、访问令牌、备份文件或包含敏感信息的截图。

请通过GitHub仓库的“Security”页面使用私密漏洞报告功能。报告中建议包含：

- 受影响版本或提交
- 复现步骤和影响范围
- 最小化且已脱敏的请求或日志
- 可行的修复建议

维护者会尽快确认报告并协调修复与披露时间。修复发布前请避免公开漏洞细节。

## 支持范围

安全修复优先应用于最新版本。历史版本可能不再单独维护。

## 数据保护边界

ModelTap不使用macOS Keychain。API Key通过本地AES-GCM加密保存，但数据库和加密密钥均属于同一macOS用户，不能抵御已经取得该用户文件读取权限的攻击者。

Markdown导出包含明文API Key，应视为敏感文件。复制API Key时应用会请求剪贴板管理器将其视为敏感临时内容，但第三方剪贴板管理器可能忽略该标记。
